
// Sintaxis 1
const numbers: number[] = [1, 2, 3, 4, 5];
numbers.push(6)

// Tuple
const fruits: [string, string, string] = ["apple", "banana", "orange"]

// Sintaxis 2
const numbers2: Array<number> = [1, 4, 4, 4,]
numbers2.push(6)

const empty = []

// Mixto array
const mixto: (number | null)[] = [1, null]



