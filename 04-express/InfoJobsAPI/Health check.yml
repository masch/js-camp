info:
  name: Health check
  type: http
  seq: 4

http:
  method: GET
  url: "{{DOMAIN}}/health"
  headers:
    - name: Origin
      value: http://localhost:5173
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
